version: '3'
 
services:
  web:
    ports:
      - "8000:8000"
 
  postgres:
    ports:
      - "5432:5432"


# docker-compose build
# docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -f docker-compose.port.yml up -d